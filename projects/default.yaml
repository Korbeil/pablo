# PABLO-wide defaults. Not a project — the config loader skips this file
# when scanning projects/. Any project config may override these per key.
sync:
  strategy: rebase
  auto_apply: false
  interval_minutes: 30
state_polling:
  interval_minutes: 10
review:
  bot_whitelist: []
