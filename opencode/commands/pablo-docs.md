---
description: Récupère une page de documentation Confluence (via acli) — par id ou URL
subtask: true
---

## Context (auto-injected)

!`pablo docs $ARGUMENTS`

## Output

Output ONLY the page title, URL, and body returned by the command above,
unchanged. No commentary, no reformatting. If the command failed, output
its error message instead.
